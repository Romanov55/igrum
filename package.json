{
    "name": "docker-scripts",
    "version": "1.0.0",
    "scripts": {
      "dev": "sudo docker compose -f docker-compose.yml -f docker-compose.dev.yml up --build",
      "dev:back": "sudo docker compose -f docker-compose.yml -f docker-compose.dev.yml up --build -d",
      "prod": "sudo docker compose up --build",
      "prod:back": "sudo docker compose up --build -d"
    }
}
  